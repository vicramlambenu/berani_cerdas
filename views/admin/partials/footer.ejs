</main> <footer class="bg-white dark:bg-slate-900/40 border-t border-slate-200 dark:border-slate-800/80 backdrop-blur-xl py-6 mt-auto theme-transition text-xs text-slate-500 dark:text-slate-400">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 flex flex-col sm:flex-row items-center justify-between gap-3 text-center sm:text-left">
      
      <div>
        <p class="font-medium">&copy; 2026 <span class="font-bold text-slate-700 dark:text-slate-300">Tim Capstone Berani Cerdas</span>. All Rights Reserved.</p>
        <p class="text-[11px] text-slate-400 dark:text-slate-500 mt-0.5">Sistem Informasi Pengelolaan & Otomasi Beasiswa Provinsi Sulawesi Tengah.</p>
      </div>

      <div class="flex items-center gap-3">
        <div class="flex items-center gap-1.5 px-2.5 py-1 rounded-full bg-emerald-500/10 border border-emerald-500/20 text-emerald-500 font-semibold tracking-wider uppercase text-[10px]">
          <span class="w-1.5 h-1.5 rounded-full bg-emerald-500 animate-pulse"></span>
          Gemini 2.0 Flash Lite Connected
        </div>
        <div class="hidden sm:block text-[11px] font-mono text-slate-400 dark:text-slate-500">
          v1.4.2-stable
        </div>
      </div>

    </div>
  </footer>

  <script>
    (function() {
      const themeToggleBtn = document.getElementById('theme-toggle');
      const lightIcon = document.getElementById('theme-toggle-light-icon');
      const darkIcon = document.getElementById('theme-toggle-dark-icon');

      if (!themeToggleBtn || !lightIcon || !darkIcon) return;

      // 1. SINKRONISASI AWAL: Memastikan teks/ikon tombol searah dengan class DOM <html>
      if (document.documentElement.classList.contains('dark')) {
        lightIcon.classList.remove('hidden');
        darkIcon.classList.add('hidden');
      } else {
        darkIcon.classList.remove('hidden');
        lightIcon.classList.add('hidden');
      }

      // 2. EVENT LISTENER: Aksi klik sakelar tema ramah mata untuk orang tua
      themeToggleBtn.addEventListener('click', function() {
        // Efek transisi ikon di dalam tombol
        lightIcon.classList.toggle('hidden');
        darkIcon.classList.toggle('hidden');

        // Evaluasi kondisi class DOM untuk eksekusi pertukaran tema global
        if (document.documentElement.classList.contains('dark')) {
          document.documentElement.classList.remove('dark');
          localStorage.setItem('color-theme', 'light');
        } else {
          document.documentElement.classList.add('dark');
          localStorage.setItem('color-theme', 'dark');
        }
      });
    })();
  </script>

</body>
</html>